specVersion: "v1beta"
serviceId: "chatroomfirebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chatroomfirebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
